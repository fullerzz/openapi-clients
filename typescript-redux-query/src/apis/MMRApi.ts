// tslint:disable
/**
 * Deadlock API
 *  ## API Clients  We have auto generated and updated clients for many languages. You can find them here: [https://github.com/deadlock-api/openapi-clients](https://github.com/deadlock-api/openapi-clients)  ## Support the Deadlock API  Whether you\'re building your own database, developing data science projects, or enhancing your website with game and player analytics, the Deadlock API has the data you need.  Your sponsorship helps keep this resource open, free and future-proof for everyone. By supporting the Deadlock API, you will enable continued development, new features and reliable access for developers, analysts and streamers worldwide.  Help us continue to provide the data you need - sponsor the Deadlock API today!  **-> You can Sponsor the Deadlock API on [Patreon](https://www.patreon.com/c/user?u=68961896) or [GitHub](https://github.com/sponsors/raimannma)**  ## Disclaimer _deadlock-api.com is not endorsed by Valve and does not reflect the views or opinions of Valve or anyone officially involved in producing or managing Valve properties. Valve and all associated properties are trademarks or registered trademarks of Valve Corporation_         
 *
 * The version of the OpenAPI document: 0.1.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


import { HttpMethods, QueryConfig, ResponseBody, ResponseText } from 'redux-query';
import * as runtime from '../runtime';
import {
    DistributionEntry,
    DistributionEntryFromJSON,
    DistributionEntryToJSON,
    MMRHistory,
    MMRHistoryFromJSON,
    MMRHistoryToJSON,
} from '../models';

export interface HeroMmrRequest {
    accountIds: Array<number>;
    heroId: number;
    maxMatchId?: number;
}

export interface HeroMmrDistributionRequest {
    heroId: number;
    minUnixTimestamp?: number;
    maxUnixTimestamp?: number;
    minDurationS?: number;
    maxDurationS?: number;
    isHighSkillRangeParties?: boolean;
    isLowPriPool?: boolean;
    isNewPlayerPool?: boolean;
    minMatchId?: number;
    maxMatchId?: number;
}

export interface HeroMmrHistoryRequest {
    accountId: number;
    heroId: number;
}

export interface MmrRequest {
    accountIds: Array<number>;
    maxMatchId?: number;
}

export interface MmrDistributionRequest {
    minUnixTimestamp?: number;
    maxUnixTimestamp?: number;
    minDurationS?: number;
    maxDurationS?: number;
    isHighSkillRangeParties?: boolean;
    isLowPriPool?: boolean;
    isNewPlayerPool?: boolean;
    minMatchId?: number;
    maxMatchId?: number;
}

export interface MmrHistoryRequest {
    accountId: number;
}


/**
 *  Deprecated. Valve reports a single account-wide rank, not a per-hero one, so this returns each player\'s rank on their latest ranked match played on that hero.  Use `/v1/players/{account_id}/rank` instead. 
 * Batch Hero MMR (Deprecated)
 */
function heroMmrRaw<T>(requestParameters: HeroMmrRequest, requestConfig: runtime.TypedQueryConfig<T, Array<MMRHistory>> = {}): QueryConfig<T> {
    if (requestParameters.accountIds === null || requestParameters.accountIds === undefined) {
        throw new runtime.RequiredError('accountIds','Required parameter requestParameters.accountIds was null or undefined when calling heroMmr.');
    }

    if (requestParameters.heroId === null || requestParameters.heroId === undefined) {
        throw new runtime.RequiredError('heroId','Required parameter requestParameters.heroId was null or undefined when calling heroMmr.');
    }

    let queryParameters = null;

    queryParameters = {};


    if (requestParameters.accountIds) {
        queryParameters['account_ids'] = requestParameters.accountIds;
    }


    if (requestParameters.maxMatchId !== undefined) {
        queryParameters['max_match_id'] = requestParameters.maxMatchId;
    }

    const headerParameters : runtime.HttpHeaders = {};


    const { meta = {} } = requestConfig;

    const config: QueryConfig<T> = {
        url: `${runtime.Configuration.basePath}/v1/players/mmr/{hero_id}`.replace('{hero_id}', encodeURIComponent(String(requestParameters.heroId))),
        meta,
        update: requestConfig.update,
        queryKey: requestConfig.queryKey,
        optimisticUpdate: requestConfig.optimisticUpdate,
        force: requestConfig.force,
        rollback: requestConfig.rollback,
        options: {
            method: 'GET',
            headers: headerParameters,
        },
        body: queryParameters,
    };

    const { transform: requestTransform } = requestConfig;
    if (requestTransform) {
        config.transform = (body: ResponseBody, text: ResponseBody) => requestTransform(body.map(MMRHistoryFromJSON), text);
    }

    return config;
}

/**
*  Deprecated. Valve reports a single account-wide rank, not a per-hero one, so this returns each player\'s rank on their latest ranked match played on that hero.  Use `/v1/players/{account_id}/rank` instead. 
* Batch Hero MMR (Deprecated)
*/
export function heroMmr<T>(requestParameters: HeroMmrRequest, requestConfig?: runtime.TypedQueryConfig<T, Array<MMRHistory>>): QueryConfig<T> {
    return heroMmrRaw(requestParameters, requestConfig);
}

/**
 *  Deprecated. Valve reports a single account-wide rank, not a per-hero one, so this counts players by the rank they had on their latest ranked match played on that hero.  Use `/v1/analytics/badge-distribution` instead. 
 * Hero MMR Distribution (Deprecated)
 */
function heroMmrDistributionRaw<T>(requestParameters: HeroMmrDistributionRequest, requestConfig: runtime.TypedQueryConfig<T, Array<DistributionEntry>> = {}): QueryConfig<T> {
    if (requestParameters.heroId === null || requestParameters.heroId === undefined) {
        throw new runtime.RequiredError('heroId','Required parameter requestParameters.heroId was null or undefined when calling heroMmrDistribution.');
    }

    let queryParameters = null;

    queryParameters = {};


    if (requestParameters.minUnixTimestamp !== undefined) {
        queryParameters['min_unix_timestamp'] = requestParameters.minUnixTimestamp;
    }


    if (requestParameters.maxUnixTimestamp !== undefined) {
        queryParameters['max_unix_timestamp'] = requestParameters.maxUnixTimestamp;
    }


    if (requestParameters.minDurationS !== undefined) {
        queryParameters['min_duration_s'] = requestParameters.minDurationS;
    }


    if (requestParameters.maxDurationS !== undefined) {
        queryParameters['max_duration_s'] = requestParameters.maxDurationS;
    }


    if (requestParameters.isHighSkillRangeParties !== undefined) {
        queryParameters['is_high_skill_range_parties'] = requestParameters.isHighSkillRangeParties;
    }


    if (requestParameters.isLowPriPool !== undefined) {
        queryParameters['is_low_pri_pool'] = requestParameters.isLowPriPool;
    }


    if (requestParameters.isNewPlayerPool !== undefined) {
        queryParameters['is_new_player_pool'] = requestParameters.isNewPlayerPool;
    }


    if (requestParameters.minMatchId !== undefined) {
        queryParameters['min_match_id'] = requestParameters.minMatchId;
    }


    if (requestParameters.maxMatchId !== undefined) {
        queryParameters['max_match_id'] = requestParameters.maxMatchId;
    }

    const headerParameters : runtime.HttpHeaders = {};


    const { meta = {} } = requestConfig;

    const config: QueryConfig<T> = {
        url: `${runtime.Configuration.basePath}/v1/players/mmr/distribution/{hero_id}`.replace('{hero_id}', encodeURIComponent(String(requestParameters.heroId))),
        meta,
        update: requestConfig.update,
        queryKey: requestConfig.queryKey,
        optimisticUpdate: requestConfig.optimisticUpdate,
        force: requestConfig.force,
        rollback: requestConfig.rollback,
        options: {
            method: 'GET',
            headers: headerParameters,
        },
        body: queryParameters,
    };

    const { transform: requestTransform } = requestConfig;
    if (requestTransform) {
        config.transform = (body: ResponseBody, text: ResponseBody) => requestTransform(body.map(DistributionEntryFromJSON), text);
    }

    return config;
}

/**
*  Deprecated. Valve reports a single account-wide rank, not a per-hero one, so this counts players by the rank they had on their latest ranked match played on that hero.  Use `/v1/analytics/badge-distribution` instead. 
* Hero MMR Distribution (Deprecated)
*/
export function heroMmrDistribution<T>(requestParameters: HeroMmrDistributionRequest, requestConfig?: runtime.TypedQueryConfig<T, Array<DistributionEntry>>): QueryConfig<T> {
    return heroMmrDistributionRaw(requestParameters, requestConfig);
}

/**
 *  Deprecated. Valve reports a single account-wide rank, not a per-hero one, so this returns the player\'s rank at the end of each ranked match they played on that hero.  Use the `ranked_display_badge` and `ranked_delta` fields of `/v1/players/{account_id}/match-history` instead. 
 * Hero MMR History (Deprecated)
 */
function heroMmrHistoryRaw<T>(requestParameters: HeroMmrHistoryRequest, requestConfig: runtime.TypedQueryConfig<T, Array<MMRHistory>> = {}): QueryConfig<T> {
    if (requestParameters.accountId === null || requestParameters.accountId === undefined) {
        throw new runtime.RequiredError('accountId','Required parameter requestParameters.accountId was null or undefined when calling heroMmrHistory.');
    }

    if (requestParameters.heroId === null || requestParameters.heroId === undefined) {
        throw new runtime.RequiredError('heroId','Required parameter requestParameters.heroId was null or undefined when calling heroMmrHistory.');
    }

    let queryParameters = null;


    const headerParameters : runtime.HttpHeaders = {};


    const { meta = {} } = requestConfig;

    const config: QueryConfig<T> = {
        url: `${runtime.Configuration.basePath}/v1/players/{account_id}/mmr-history/{hero_id}`.replace('{account_id}', encodeURIComponent(String(requestParameters.accountId))).replace('{hero_id}', encodeURIComponent(String(requestParameters.heroId))),
        meta,
        update: requestConfig.update,
        queryKey: requestConfig.queryKey,
        optimisticUpdate: requestConfig.optimisticUpdate,
        force: requestConfig.force,
        rollback: requestConfig.rollback,
        options: {
            method: 'GET',
            headers: headerParameters,
        },
        body: queryParameters,
    };

    const { transform: requestTransform } = requestConfig;
    if (requestTransform) {
        config.transform = (body: ResponseBody, text: ResponseBody) => requestTransform(body.map(MMRHistoryFromJSON), text);
    }

    return config;
}

/**
*  Deprecated. Valve reports a single account-wide rank, not a per-hero one, so this returns the player\'s rank at the end of each ranked match they played on that hero.  Use the `ranked_display_badge` and `ranked_delta` fields of `/v1/players/{account_id}/match-history` instead. 
* Hero MMR History (Deprecated)
*/
export function heroMmrHistory<T>(requestParameters: HeroMmrHistoryRequest, requestConfig?: runtime.TypedQueryConfig<T, Array<MMRHistory>>): QueryConfig<T> {
    return heroMmrHistoryRaw(requestParameters, requestConfig);
}

/**
 *  Deprecated. The MMR estimate is gone, this now returns the rank Valve reported for each player at the end of their latest ranked match. Players without a ranked match carrying a rank are left out.  Use `/v1/players/{account_id}/rank` instead. 
 * Batch MMR (Deprecated)
 */
function mmrRaw<T>(requestParameters: MmrRequest, requestConfig: runtime.TypedQueryConfig<T, Array<MMRHistory>> = {}): QueryConfig<T> {
    if (requestParameters.accountIds === null || requestParameters.accountIds === undefined) {
        throw new runtime.RequiredError('accountIds','Required parameter requestParameters.accountIds was null or undefined when calling mmr.');
    }

    let queryParameters = null;

    queryParameters = {};


    if (requestParameters.accountIds) {
        queryParameters['account_ids'] = requestParameters.accountIds;
    }


    if (requestParameters.maxMatchId !== undefined) {
        queryParameters['max_match_id'] = requestParameters.maxMatchId;
    }

    const headerParameters : runtime.HttpHeaders = {};


    const { meta = {} } = requestConfig;

    const config: QueryConfig<T> = {
        url: `${runtime.Configuration.basePath}/v1/players/mmr`,
        meta,
        update: requestConfig.update,
        queryKey: requestConfig.queryKey,
        optimisticUpdate: requestConfig.optimisticUpdate,
        force: requestConfig.force,
        rollback: requestConfig.rollback,
        options: {
            method: 'GET',
            headers: headerParameters,
        },
        body: queryParameters,
    };

    const { transform: requestTransform } = requestConfig;
    if (requestTransform) {
        config.transform = (body: ResponseBody, text: ResponseBody) => requestTransform(body.map(MMRHistoryFromJSON), text);
    }

    return config;
}

/**
*  Deprecated. The MMR estimate is gone, this now returns the rank Valve reported for each player at the end of their latest ranked match. Players without a ranked match carrying a rank are left out.  Use `/v1/players/{account_id}/rank` instead. 
* Batch MMR (Deprecated)
*/
export function mmr<T>(requestParameters: MmrRequest, requestConfig?: runtime.TypedQueryConfig<T, Array<MMRHistory>>): QueryConfig<T> {
    return mmrRaw(requestParameters, requestConfig);
}

/**
 *  Deprecated. The MMR estimate is gone, this now counts players by the rank Valve reported at the end of their latest ranked match within the filtered range.  Use `/v1/analytics/badge-distribution` instead. 
 * MMR Distribution (Deprecated)
 */
function mmrDistributionRaw<T>(requestParameters: MmrDistributionRequest, requestConfig: runtime.TypedQueryConfig<T, Array<DistributionEntry>> = {}): QueryConfig<T> {
    let queryParameters = null;

    queryParameters = {};


    if (requestParameters.minUnixTimestamp !== undefined) {
        queryParameters['min_unix_timestamp'] = requestParameters.minUnixTimestamp;
    }


    if (requestParameters.maxUnixTimestamp !== undefined) {
        queryParameters['max_unix_timestamp'] = requestParameters.maxUnixTimestamp;
    }


    if (requestParameters.minDurationS !== undefined) {
        queryParameters['min_duration_s'] = requestParameters.minDurationS;
    }


    if (requestParameters.maxDurationS !== undefined) {
        queryParameters['max_duration_s'] = requestParameters.maxDurationS;
    }


    if (requestParameters.isHighSkillRangeParties !== undefined) {
        queryParameters['is_high_skill_range_parties'] = requestParameters.isHighSkillRangeParties;
    }


    if (requestParameters.isLowPriPool !== undefined) {
        queryParameters['is_low_pri_pool'] = requestParameters.isLowPriPool;
    }


    if (requestParameters.isNewPlayerPool !== undefined) {
        queryParameters['is_new_player_pool'] = requestParameters.isNewPlayerPool;
    }


    if (requestParameters.minMatchId !== undefined) {
        queryParameters['min_match_id'] = requestParameters.minMatchId;
    }


    if (requestParameters.maxMatchId !== undefined) {
        queryParameters['max_match_id'] = requestParameters.maxMatchId;
    }

    const headerParameters : runtime.HttpHeaders = {};


    const { meta = {} } = requestConfig;

    const config: QueryConfig<T> = {
        url: `${runtime.Configuration.basePath}/v1/players/mmr/distribution`,
        meta,
        update: requestConfig.update,
        queryKey: requestConfig.queryKey,
        optimisticUpdate: requestConfig.optimisticUpdate,
        force: requestConfig.force,
        rollback: requestConfig.rollback,
        options: {
            method: 'GET',
            headers: headerParameters,
        },
        body: queryParameters,
    };

    const { transform: requestTransform } = requestConfig;
    if (requestTransform) {
        config.transform = (body: ResponseBody, text: ResponseBody) => requestTransform(body.map(DistributionEntryFromJSON), text);
    }

    return config;
}

/**
*  Deprecated. The MMR estimate is gone, this now counts players by the rank Valve reported at the end of their latest ranked match within the filtered range.  Use `/v1/analytics/badge-distribution` instead. 
* MMR Distribution (Deprecated)
*/
export function mmrDistribution<T>(requestParameters: MmrDistributionRequest, requestConfig?: runtime.TypedQueryConfig<T, Array<DistributionEntry>>): QueryConfig<T> {
    return mmrDistributionRaw(requestParameters, requestConfig);
}

/**
 *  Deprecated. The MMR estimate is gone, this now returns one entry per ranked match with the rank Valve reported for the player at the end of that match.  Use the `ranked_display_badge` and `ranked_delta` fields of `/v1/players/{account_id}/match-history` instead. 
 * MMR History (Deprecated)
 */
function mmrHistoryRaw<T>(requestParameters: MmrHistoryRequest, requestConfig: runtime.TypedQueryConfig<T, Array<MMRHistory>> = {}): QueryConfig<T> {
    if (requestParameters.accountId === null || requestParameters.accountId === undefined) {
        throw new runtime.RequiredError('accountId','Required parameter requestParameters.accountId was null or undefined when calling mmrHistory.');
    }

    let queryParameters = null;


    const headerParameters : runtime.HttpHeaders = {};


    const { meta = {} } = requestConfig;

    const config: QueryConfig<T> = {
        url: `${runtime.Configuration.basePath}/v1/players/{account_id}/mmr-history`.replace('{account_id}', encodeURIComponent(String(requestParameters.accountId))),
        meta,
        update: requestConfig.update,
        queryKey: requestConfig.queryKey,
        optimisticUpdate: requestConfig.optimisticUpdate,
        force: requestConfig.force,
        rollback: requestConfig.rollback,
        options: {
            method: 'GET',
            headers: headerParameters,
        },
        body: queryParameters,
    };

    const { transform: requestTransform } = requestConfig;
    if (requestTransform) {
        config.transform = (body: ResponseBody, text: ResponseBody) => requestTransform(body.map(MMRHistoryFromJSON), text);
    }

    return config;
}

/**
*  Deprecated. The MMR estimate is gone, this now returns one entry per ranked match with the rank Valve reported for the player at the end of that match.  Use the `ranked_display_badge` and `ranked_delta` fields of `/v1/players/{account_id}/match-history` instead. 
* MMR History (Deprecated)
*/
export function mmrHistory<T>(requestParameters: MmrHistoryRequest, requestConfig?: runtime.TypedQueryConfig<T, Array<MMRHistory>>): QueryConfig<T> {
    return mmrHistoryRaw(requestParameters, requestConfig);
}

